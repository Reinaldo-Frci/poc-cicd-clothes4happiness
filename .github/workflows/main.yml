name: Deploy para o servidor

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Atualizar repositório no servidor
        run: |
          cd ~/poc/poc-cicd-clothes4happiness
          git pull origin main
          echo "Deploy concluído em $(date)"
